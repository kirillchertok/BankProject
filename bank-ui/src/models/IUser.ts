export interface IUser{
    name: string;
    secondname: string, 
    phoneNumber: string, 
    email: string, 
    tfAuth: boolean, 
    role: string,
    passportNumber: string,
    birthdayDate: string,
    passportId: string,
}